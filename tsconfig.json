{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "//": "scripts/ is excluded because `next build` typechecks everything this file includes, and the operator tools in there import `playwright`, which is a local-only tool and not a dependency of the deployed app. Leaving them in failed the Vercel build with \"Cannot find module 'playwright'\" while building fine on a laptop that happened to have it. The scripts are still typechecked — by tsconfig.scripts.json, via `npm run typecheck`.",
  "exclude": ["node_modules", "scripts"]
}
