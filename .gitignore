node_modules/
.next/
out/
build/
.DS_Store
*.pem

.env
.env*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
*.tsbuildinfo
next-env.d.ts
khata-backup-*.json
.next-*/

# Screenshots from scripts/responsive-check.mjs, and the separate build
# directory a verification build uses so it cannot kill a running dev server.
.responsive/
.next-verify/
.dev.log

# QA reports stay local: they hold phone numbers, test credentials and screenshots,
# and this repository is public. Never commit anything under qa-report/.
qa-report/
